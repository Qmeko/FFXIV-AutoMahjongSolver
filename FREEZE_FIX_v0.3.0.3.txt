Doman Mahjong Solver Debug + Mortal AI v0.3.0.3

Startup freeze fix
- Mortal Python/PyTorch/model startup now runs in a background task.
- A synthetic hand prewarms the local model immediately after plugin load.
- The real Mortal process stays alive after warmup.
- The Dalamud framework thread never waits for process startup or JSONL inference.
- Auto-play pauses without clicking while Mortal is still warming up.
- Call prompts are not auto-passed during warmup.
- The same state is retried automatically when the Mortal decision becomes ready.
- Normal timeout/error fallback to the built-in policy remains enabled.
