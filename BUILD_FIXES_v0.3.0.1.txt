Doman Mahjong Solver Debug Mortal OneClickBuild v0.3.0.1

Build launcher fix:
- Replaced the fragile inline PowerShell -Command wrapper.
- Added build-tools\Run-OneClick.ps1 and invoke it through PowerShell -File.
- PowerShell stream merging now occurs inside the .ps1 file, so cmd.exe never parses *>&1 or 2>&1.
- Preserves live console output and BUILD_DEBUG_PLUGIN.log.

Mortal integration retained:
- Installs Akagi-MjaiBot-Mortal release4p runtime automatically.
- Installs managed Python 3.12 and CPU PyTorch through uv.
- Performs a real Mortal model/mjai round-trip smoke test.
- Maps discard, pass, riichi, tsumo, ron, pon, chi, ankan, daiminkan and kakan.
- Rejects illegal AI actions and falls back to the built-in policy.
